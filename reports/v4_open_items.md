# v4 Open Items

## Build blockers

None at the governance level.

## Remaining non-blocking notes

- `CR_EU_HY` is a valid first-build admit, but it should be documented in implementation notes as a higher-risk sleeve than `FI_EU_GOVT` and `CR_EU_IG`.
- `FI_JP_GOVT` remains a future candidate if a clean long-history public target rule is identified.
- `FI_CN_GOVT` remains deferred unless a truly clean pure-sovereign investable rule becomes available.
- Prompt 4 should preserve a strict version boundary so `v3_long_horizon_china` remains frozen and active until `v4` is built and reviewed.
